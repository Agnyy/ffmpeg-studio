/** FFmpeg AV_NOPTS_VALUE (INT64_MIN) — local copy to avoid ESM require in main bundle. */
export const AV_NOPTS_VALUE = -9223372036854775808n;

/** FFmpeg AVSEEK_FLAG_BACKWARD */
export const AVSEEK_FLAG_BACKWARD = 1;
